#compdef aws

(( $+commands[aws_completer] )) || return 1

unfunction _aws 2>/dev/null
autoload -U +X bashcompinit && bashcompinit
complete -C "$(command -v aws_completer)" aws
compdef _bash_complete aws

_bash_complete "$@"
