# TODO: merge with system/path.zsh?

# add common paths for homebrew
#export PATH="/usr/local/bin:/usr/local/sbin:/usr/local/mysql/bin:/usr/local/git/bin:/opt/local/bin:/opt/local/sbin:$PATH"

# add stuff to the manual path
#export MANPATH="/usr/local/man:/usr/local/mysql/man:/usr/local/git/man:$MANPATH"

# add custom bin folders to path
#export PATH="$ZSH/bin:$HOME/bin:$HOME/.bin:$PATH"

# If the mediatools repo exists, add it to the path
#if [[ -d "$HOME/Projects/jacobwg/mediatools" ]]; then
#  export PATH="$HOME/Projects/jacobwg/mediatools/bin:$PATH"
#fi

# If MAMP is installed, add the latest PHP to the path
#if [[ -d /Applications/MAMP ]]; then
#  export PATH="/Applications/MAMP/bin/php/$(/bin/ls /Applications/MAMP/bin/php/ | /usr/bin/tail -1)/bin:$PATH"
#fi
